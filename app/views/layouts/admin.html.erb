<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Admin Panel" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css", crossorigin: "anonymous" %>
    <%= stylesheet_link_tag :application, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <%= yield :head %>
  </head>
  <body>
    <div class="container-fluid">
      <div class="row min-vh-100">
        <nav class="col-md-2 d-none d-md-block bg-light sidebar py-4">
          <div class="position-sticky">
            <ul class="nav flex-column">
              <li class="nav-item mb-2">
                <%= link_to "Dashboard", admin_dashboard_index_path, class: "nav-link" %>
              </li>
              <li class="nav-item mb-2">
                <%= link_to "Users", admin_users_path, class: "nav-link" %>
              </li>
              <% if defined?(@user) && @user.present? %>
                <li class="nav-item mb-2">
                  <%= link_to "Credit Topups", credit_topups_admin_user_path(@user), class: "nav-link" %>
                </li>
                <li class="nav-item mb-2">
                  <%= link_to "Credit Spents", credit_spents_admin_user_path(@user), class: "nav-link" %>
                </li>
              <% end %>
              <li class="nav-item mb-2">
                <%= link_to "Waiting List", admin_waiting_list_index_path, class: "nav-link" %>
              </li>
            </ul>
          </div>
        </nav>
        <main class="col-md-10 ms-sm-auto px-md-4 py-4">
          <%= yield %>
        </main>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
  </body>
</html>
